// カメラの表示サイズ
export const CAMERA_DISPLAY_SIZE = {
    WIDTH: 400,
    HEIGHT: 400
} as const;

// キャンバスのサイズ
export const CANVAS_SIZE = {
    WIDTH: 720,
    HEIGHT: 720
} as const;

// カメラの解像度
export const CAMERA_RESOLUTION = {
    WIDTH: 1280,
    HEIGHT: 720
} as const;